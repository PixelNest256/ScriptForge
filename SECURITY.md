# Security Policy

## 報告方法

セキュリティ上の問題は GitHub の公開 Issue ではなく、リポジトリオーナーへ非公開で連絡してください（Security Advisories またはメール）。

## 脅威モデル

ScriptForge はユーザーが AI 生成またはインポートした JavaScript を任意の Web ページで実行します。主なリスク:

- プロンプトインジェクションによる過剰権限コードの生成
- 静的解析の回避（難読化）
- 保存済みスクリプトの改ざん

## 緩和策

1. **静的解析** — AST による危険パターン検出と権限分類
2. **権限確認 UI** — 実行前の人間による承認
3. **ハッシュ検証** — 保存時 SHA-256、注入前に照合
4. **Isolated World** — `chrome.scripting.executeScript` の `world: 'ISOLATED'`
5. **MV3 CSP** — 拡張機能ページでの `eval` 禁止

## 既知の限界

- 完全な静的解析は不可能（難読化・反射的呼び出しなど）
- ユーザーが承認した場合、ネットワーク送信や Cookie 読み取りは許可される

## 推奨事項（ユーザー）

- 不明なソースの `.user.js` をインポートしない
- 権限確認画面で 🔴 高リスク権限を慎重に確認する
- API キーは拡張機能の設定のみに保存し、共有しない
